{
    "name": "memo-init-spec",
    "version": "0.1.0",
    "description": "memo-init Specification — induktive RFC-Spec des Memo-Systems + Sub-Spec Workbench",
    "type": "module",
    "license": "MIT",
    "engines": {
        "node": ">=22"
    },
    "scripts": {
        "generate-refs": "node scripts/generate-refs.mjs",
        "generate-docs-payload": "node scripts/generate-docs-payload.mjs",
        "generate-manifest": "node scripts/generate-manifest.mjs",
        "generate-llms-txt": "node scripts/generate-llms-txt.mjs",
        "spec-drift": "memo spec drift-check",
        "generate-bridge": "node scripts/generate-bridge.mjs",
        "audit-spec-quality": "node scripts/audit-spec-quality.mjs",
        "check-bridge-inverse": "node scripts/check-bridge-inverse.mjs",
        "check-dist-parity": "node scripts/check-dist-parity.mjs",
        "spec-doctor": "memo spec doctor",
        "test": "node scripts/audit-spec-quality.mjs && node scripts/check-bridge-inverse.mjs && node scripts/check-skill-specs.mjs",
        "build": "npm run generate-refs && npm run generate-docs-payload && npm run generate-bridge && npm run generate-manifest && npm run generate-llms-txt && npm run check-bridge-inverse && npm run spec-drift"
    },
    "devDependencies": {
        "ajv": "^8.20.0"
    }
}
