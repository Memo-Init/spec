name: Notify docs site

# Fan-out: when the spec's generated artifacts change, tell the website
# (Memo-Init/memo-init.github.io) to rebuild via a repository_dispatch event.
#
# Memo 053 (Kap 1, Fix A.2 — race eliminieren): this fires ONLY on `generated/**`,
# never on `spec/**` directly. A source push first triggers generate.yml, which
# regenerates `generated/` and commits it (with the PAT, so this workflow re-triggers);
# only that bot commit fires the notify -> exactly ONE site deploy, from the POST-regen
# state. Firing on `spec/**` too caused a second, earlier deploy that built from the
# PRE-regen tree (the stale "first deploy" race). The site build reads chapters directly
# from the spec HEAD, and every chapter edit changes generated/llms-full.txt, so a
# `generated/**`-only trigger still covers all content changes.

on:
  push:
    branches: [main]
    paths:
      - 'generated/**'
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch spec-updated
        run: |
          curl -sf -X POST \
            -H "Authorization: Bearer ${{ secrets.PUBLISH_SPEC_CHANGES }}" \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            https://api.github.com/repos/Memo-Init/memo-init.github.io/dispatches \
            -d '{"event_type":"spec-updated"}'
