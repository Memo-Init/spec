{
    "toolContract": {
        "note": "core + roles per REV-07 Kap 3 (F17=A). The values still await cross-check against the WI-2-05 tool-inventory snapshot (Phase 2, not yet present in the workshop); when it lands, reconcile core.required against it.",
        "core": {
            "required": [ "Bash", "Read", "Write", "Edit", "Skill", "ToolSearch" ]
        },
        "roles": {
            "orchestrator": {
                "add": [ "Agent", "SendMessage", "Workflow" ],
                "remove": [],
                "taskCrud": {
                    "mode": "gate-dependent",
                    "note": "Task-CRUD is time-variant (T038): present up to 2026-07-09, null from 2026-07-10. The pre-flight Probe decides per session, so it is a gate-dependent marker rather than a hard core/add entry."
                }
            },
            "worker": {
                "add": [ "StructuredOutput" ],
                "remove": [ "Agent", "TaskCRUD" ]
            },
            "user": {
                "add": [ "AskUserQuestion" ],
                "remove": []
            }
        }
    },
    "config": {
        "env": [ "DISABLE_GROWTHBOOK" ],
        "hooks": [ "PreToolUse", "PostToolUse", "SessionStart", "Stop" ],
        "permissions": [ "allow", "deny", "ask" ],
        "skills": [ "skillDirs" ]
    },
    "adapter": {
        "kind": "cli",
        "entrypoint": "claude",
        "notes": "Claude Code CLI harness, driven as a local CLI process."
    }
}
